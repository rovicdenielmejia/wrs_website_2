# WRS – local development env (copy to .env.development.local or use vercel env pull)
# Do not commit real values. .env.development.local is gitignored.

# Postgres connection (from Neon or other provider; set in Vercel for production)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
